import React from 'react';
import { View, Text } from 'react-native';

const FlightPlanSecond = () => {
  return (
    <View style={{ height: '100%', justifyContent: 'center'}}>
      <Text style={{fontSize: 40}}>Segunda pantalla</Text>
    </View>

  )
};

export default FlightPlanSecond;